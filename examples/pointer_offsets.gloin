// A nullable pointer can step through elements of one live allocation.
def main() -> i32 {
    def mut values: [i32; 3] = {10, 20, 12};
    def first: *i32 = &values[0];
    def second: *i32 = first + 1;
    def third: *i32 = second + 1;
    return *second + *third;
}
