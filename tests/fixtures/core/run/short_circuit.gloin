def trap() -> bool { def x: i32 = 1 / 0; return true; }
def main() -> i32 {
    if false && trap() { return -1; }
    if !(true || trap()) { return -2; }
    if true && (false || true) { return 42; }
    return -3;
}
